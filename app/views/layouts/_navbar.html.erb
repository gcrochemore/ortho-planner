<nav class="navbar navbar-fixed-top navbar-default">
  <div class="container-fluid" id="header_container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Ortho-planner', '/', class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <% if user_signed_in?  %>
        <ul class="nav navbar-nav ">        
            <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">MyOrthoPlanner
            <span class="caret"></span></a>
            <ul class="dropdown-menu">
            <% if can? :read, Patient %>
              <li><%= link_to('Liste des patients', patients_path)  %> </li>
            <% end %>
            <% if can? :read, Therapy %>
              <li><%= link_to('Liste des therapies', therapies_path)  %> </li>
            <% end %>
            <% if can? :read, Session %>
              <li><%= link_to('Liste des séances', sessions_path)  %> </li>
            <% end %>
            </ul>
          </li>
        </ul>
        <ul class="nav navbar-nav ">        
            <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Dictionnaires
            <span class="caret"></span></a>
            <ul class="dropdown-menu">
            <% if can? :read, Practitioner %>
              <li><%= link_to('Liste des praticiens', practitioners_path)  %></li>
            <% end %>
            <% if can? :read, PractitionerProfession %>
              <li><%= link_to('Liste des type de praticiens', practitioner_professions_path)  %></li>
            <% end %>
            <% if can? :read, Office %>
              <li><%= link_to('Liste des offices', offices_path)  %> </li>
            <% end %>
            <% if can? :read, Pathology %>
              <li><%= link_to('Liste des pathologies', pathologies_path)  %> </li>
            <% end %>
            <% if can? :read, Act %>
              <li><%= link_to('Liste des actes', acts_path)  %> </li>
            <% end %>
            </ul>
          </li>
        </ul>
        <% if current_user.has_role? :admin %>
          <ul class="nav navbar-nav ">        
              <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">Administration
              <span class="caret"></span></a>
              <ul class="dropdown-menu">
              <% if can? :read, User %>
                <li><%= link_to('Liste des utilisateurs', users_path)  %> </li>
                <li><%= link_to('Paper trail', paper_trail_versions_path)  %> </li>
              <% end %>
              </ul>
            </li>
          </ul>
        <% end %>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#"><i class='glyphicon glyphicon-engrenage glyphicon glyphicon-engrenage-white'></i><%= current_user.email %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= Time.zone.now.to_s %><br><%= Time.zone.to_s %><bR><%= Time.zone.now.utc_offset.to_s %></li>
            </ul>
          </li>
          <li><%= link_to('Se déconnecter', destroy_user_session_path, method: :delete) %></li>
        <% else %>
          <li><%= link_to('Login', new_user_session_path)  %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->

    


  </div>
</nav>